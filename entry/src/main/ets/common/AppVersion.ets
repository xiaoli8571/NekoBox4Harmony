/**
 * 应用版本单一来源。
 * 升级版本时必须与 AppScope/app.json5 的 versionName/versionCode 同步修改。
 */
export const APP_VERSION: string = '1.7.7';
export const APP_VERSION_CODE: number = 1001707;
export const APP_VERSION_LABEL: string = `v${APP_VERSION} (${APP_VERSION_CODE})`;
