# NekoBox Harmony NEXT 开发路线图（Android NekoBox 对齐版）

## 项目目标

对齐对象：NekoBoxForAndroid

核心目标：
- 功能对齐 Android NekoBox
- 页面布局对齐 Android NekoBox
- 操作路径对齐 Android NekoBox
- Harmony NEXT 平台特性等价实现
- 不修改 Sing-box 内核（特殊说明除外）

## 版本对照表（发布号以此为准，勿用历史编号）

| 发布版本 | versionCode | 内容（对应章节） |
|----------|-------------|------------------|
| v1.7.0   | 1001700     | 第二章 基础架构整理 + 第三章 节点管理系统对齐（小节沿用 1.6.1~1.6.5 原编号） |
| v1.8.0   | 1001800     | 第四章 订阅系统对齐 |
| v1.9.0   | 1001900     | 第五章 路由系统对齐 |
| v2.0.0   | 1002000     | 第六章 分应用代理 |
| v2.1.0   | 1002100     | 第七章 设置与系统集成 |
| v2.2.0   | 1002200     | 第八章 体验增强（建议新增） |

规则：每个发布版本独立可发布、真机双模式回归通过后，才派发下一个章节。

## 开发原则

### 功能边界

本项目负责：UI、配置管理、节点管理、订阅管理、路由管理、VPN 生命周期管理、Clash API 交互。

本项目不负责：修改 Sing-box 内核、修改 Mihomo 内核、修改协议实现逻辑。

### 每阶段必须独立可发布

- 功能完整
- 配置兼容旧版本
- 真机验证通过
- 不依赖后续版本功能

### 双场景验证

规则模式与全局模式均需验证：
- DNS解析
- 国内直连
- 国外代理
- 节点切换
- 断开重连

### Android 行为一致原则

允许实现方式不同，但必须保证用户侧行为一致。

## 二、v1.7.0 基础架构整理（原「v1.5.5」，随 v1.7.0 顺带完成）

### 模块拆分
```
common/
├─ manager
├─ service
├─ storage
├─ model
└─ event
```

### 统一连接状态机
```
IDLE
CONNECTING
CONNECTED
DISCONNECTING
TESTING
```

## 三、v1.7.0 节点管理系统对齐（原「v1.6.0」；小节编号 1.6.x 沿用原规划）

### 1.6.1 顶部状态卡片

显示：
- 连接状态
- 当前节点
- 运行时长
- 实时速率
- 累计流量

验收：
- 状态颜色正确
- 数据实时同步
- 断开自动归零

### 1.6.2 节点卡片增强

支持：
- 协议徽标
- 延迟彩色圆点
- 来源域名显示
- 置顶标签

### 1.6.3 长按菜单

- 分享
- 编辑
- URL测速
- 置顶
- 复制
- 删除

新增：LinkExporter.ets

### 1.6.4 URL Test 对齐

基于 Clash API：

```http
GET /proxies/{name}/delay
```

新增：
- 批量测速
- 自动测速
- 延迟排序

### 1.6.5 FAB 导入菜单

支持：
- 剪贴板导入
- 扫码导入
- 手动创建
- 文件导入

## 四、v1.8.0 订阅系统对齐（原「v1.7.0」）

### 7.1 分组 Tab

- 全部
- 手动配置
- 各订阅分组

### 7.2 订阅详情页

显示：
- URL
- 流量
- 到期时间
- 更新时间
- 更新间隔

### 7.3 自动更新系统

- 启动更新
- 周期更新
- 手动更新
- 连续失败3次自动暂停

### 7.4 订阅健康检查

检查：
- 空订阅
- 格式异常
- 节点数量异常下降
- 全部节点失效

## 五、v1.9.0 路由系统对齐（原「v1.8.0」）

### 路由预设

- 全局代理
- 绕过大陆
- 仅直连
- 广告拦截
- 自定义

### 自定义规则

支持：
- DOMAIN
- DOMAIN_SUFFIX
- DOMAIN_KEYWORD
- IP_CIDR
- PORT
- PROCESS
- RULE_SET

动作：

## 六、v2.0.0 分应用代理（原「v2.0.0」）

### AppList.ets

支持：
- 应用图标
- 应用名称
- BundleName
- 搜索
- 多选

### Android 同款交互

- 全选
- 反选
- 用户应用分组
- 系统应用分组

## 七、v2.1.0 设置与系统集成（原「v2.0.0」）

### 主题

- 跟随系统
- 浅色
- 深色

### 多语言

- 中文
- English

### 通知快捷操作

- 断开连接
- 切换节点

### 分享导入

接收 text/plain 分享内容。

### 桌面卡片

显示：
- 状态
- 节点
- 实时速率

### 文件备份

- profiles.json
- subscriptions.json
- settings.json

### 日志中心

- INFO
- WARNING
- ERROR
- 导出日志
- 搜索日志

### 平台限制

Harmony NEXT 普通应用不支持开机自启。

## 八、v2.2.0 体验增强（原「v2.1.0」，建议新增）

### 节点收藏

支持 Favorite 分组。

### 最近使用节点

自动记录最近 10 个节点。

### 一键最佳节点

测速后自动切换最低延迟节点。

### 节点可用性监控

后台 URL Test 自动检测。

### 配置诊断工具

输出：
- VPN状态
- DNS状态
- 路由状态
- 节点状态
- Geo状态

## 最终目标

- 90% Android 功能覆盖率
- 95% 页面覆盖率
- 100% 核心代理能力覆盖率
- Harmony NEXT 原生体验完善
